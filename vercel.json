{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/health", "dest": "/api/health.ts" },
    { "src": "/mcp", "dest": "/api/mcp.ts" },
    { "src": "/(.*)", "dest": "/api/mcp.ts" }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
